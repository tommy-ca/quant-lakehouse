DELETE FROM "registry"."venues" as d WHERE EXISTS (SELECT 1 FROM "registry_staging"."venues" as s WHERE d."venue_id" = s."venue_id")
INSERT INTO "registry"."venues"("venue_id", "name", "market_type", "base_url", "status", "timezone", "fetched_at", "_dlt_load_id", "_dlt_id")                  SELECT "venue_id", "name", "market_type", "base_url", "status", "timezone", "fetched_at", "_dlt_load_id", "_dlt_id"                     FROM (                         SELECT ROW_NUMBER() OVER (partition BY "venue_id" ORDER BY (SELECT NULL)) AS _dlt_dedup_rn, "venue_id", "name", "market_type", "base_url", "status", "timezone", "fetched_at", "_dlt_load_id", "_dlt_id"                         FROM "registry_staging"."venues"                     ) AS _dlt_dedup_numbered WHERE _dlt_dedup_rn = 1 AND (1 = 1)          